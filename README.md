# 🖼️ AI Watermark Remover & Upscaler

Автоматизированный пайплайн для обработки фотографий: удаление вотермарок, AI-апскейл и подготовка для маркетплейсов (Wildberries).

## ✨ Возможности

- **Удаление вотермарок**: Использует `IOPaint` (модель LaMa) для чистого удаления объектов.
- **AI Апскейл**: Интеграция с **Replicate** (модель `recraft-crisp-upscale`) для улучшения качества до 4K.
- **Подготовка для WB**: Автоматический ресайз (900x1200), кроп и конвертация в JPG.
- **Retry-логика**: Защита от сбоев сети и таймаутов API.
- **Асинхронный режим**: Параллельная обработка для скорости.

## 🛠️ Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/ваш-юзернейм/watermark-remover.git
   cd watermark-remover
   ```

2. **Создайте виртуальное окружение:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   pip install iopaint  # Устанавливается отдельно
   ```

4. **Настройте `.env`:**
   Создайте файл `.env` и добавьте ваш токен Replicate:
   ```
   REPLICATE_API_TOKEN=r8_ваш_токен_здесь
   ```

## 🚀 Использование

Положите исходные фото в папку `input/`.

### Вариант 1: Полный цикл (Вотермарка + Апскейл + WB)
```bash
python full_process.py
```

### Вариант 2: Только Апскейл + WB (если вотермарок нет)
```bash
python upscale_and_wb.py
```

### Вариант 3: Асинхронный режим (быстро)
```bash
python full_process_async.py
```

## 📂 Структура папок
- `input/` — Исходные фото
- `output/` — Фото без вотермарок
- `final_upscaled/` — Фото после апскейла
- `ready_for_wb/` — Готовые для публикации

## 📝 Лицензия
MIT
